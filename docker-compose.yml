services:
  cf-tunnel-webgui:
    image: cf-webui:latest
    container_name: cf-tunnel-webgui
    build: .
    restart: unless-stopped
    ports:
      - "8888:8000"
    volumes:
      - cf_data:/data
    environment:
      - CSRF_SECRET=${CSRF_SECRET:-}

volumes:
  cf_data:
